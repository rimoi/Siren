{% extends 'base.html.twig' %}

{% block title %}Rechercher des sirens{% endblock %}

{% block body %}
<div class="container">
    <main class="cs-form-search">
        {{ form_errors(searchSirenForm) }}
        {{ form_start(searchSirenForm) }}
            {{ form_widget(searchSirenForm.siren, {'attr': {'placeholder': 'Numéro sirene'}}) }}
            {{ form_widget(searchSirenForm.submit, {'label': 'Rechercher'}) }}
        {{ form_end(searchSirenForm) }}

        {% if result|default %}
            <div class="cs-result">
                {{ result }}
            </div>
        {% endif %}
    </main>
</div>
{% endblock %}
